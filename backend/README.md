# TransitOps Backend Directory Placeholder

This directory is reserved for all backend files (APIs, server logic, database schema, Prisma migrations, controllers).

## Structure
*   `../frontend/` : React (Vite) + TypeScript + Tailwind CSS application files.
*   `../backend/` : Express + Node.js APIs and Prisma schema files (to be added in backend integration phase).
